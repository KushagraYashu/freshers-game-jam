using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SpawnPoint : MonoBehaviour
{
    //just a spawn point class used to get the transform of the correct object (basically trying to avoid getting parent's transform component)
}
